# App

This is a minimal Node.js app added to the `app/` folder.

How to run locally:

```powershell
cd "c:\Users\sravani.vangara\Desktop\infra\assesmnet project\app"
npm install
npm start
```

The server listens on port 3000 by default and responds with JSON.




